import SwiftUI
struct CustomCreateGroupChannel: View {
    var body: some View {
        CreateGroupChannelView()
    }
}

#Preview {
    CustomCreateGroupChannel()
}

extension CustomCreateGroupChannel {
    struct SwiftUI {
        struct View {
        }
    }
    struct SubView {
        struct Builder {
        }
    }
    struct ViewConverter {
        struct Header {
        }
        struct List {
        }
    }
}
