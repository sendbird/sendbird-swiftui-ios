import SwiftUI
#if canImport(SendbirdSwiftUI)
import SendbirdSwiftUI
#endif

struct CustomOpenChannelList: View {
    var body: some View {
        OpenChannelListView()
    }
}

#Preview {
    CustomOpenChannelList()
}

extension CustomOpenChannelList {
    struct SwiftUI {
        struct View {
        }
    }
    struct SubView {
        struct Builder {
        }
    }
    struct ViewConverter {
        struct Header {
        }
        struct List {
        }
    }
}
