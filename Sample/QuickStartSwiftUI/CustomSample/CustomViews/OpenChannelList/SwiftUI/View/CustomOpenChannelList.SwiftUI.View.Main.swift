import SwiftUI

extension CustomOpenChannelList.SwiftUI.View {
    struct Main: View {
        var body: some View {
            OpenChannelListView()
        }
    }
}

#Preview {
    CustomOpenChannelList.SwiftUI.View.Main()
}
